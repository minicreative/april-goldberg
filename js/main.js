$(document).ready(() => {
    setupScrollImages()
})

function setupScrollImages() {
    let isScrolling = false;
    $("#scrollImages").on('mouseover', () => {
    })
    $("#scrollImages").on('mouseout', () => {
    })
}